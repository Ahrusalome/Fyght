using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    //Scene de choix de perso -> attribuer les perso a spawn pour la scène suivante et attribuer les healthbar manager correspondant aux healthbars
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
